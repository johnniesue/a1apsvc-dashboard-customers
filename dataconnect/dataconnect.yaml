specVersion: "v1"
serviceId: "customer-dashboard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "customer-dashboard-fdc"
        # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
